# Python virtual environment (recreate with: python3 -m venv .venv)
.venv/
__pycache__/
*.pyc

# OS / editor cruft
.DS_Store

# Full-page PDF renders (intermediate; regenerate via make_comparisons.py).
# The cropped originals (figures/original_crops/) and the side-by-side
# composites (figures/comparisons/) ARE tracked.
figures/original_pages/
